// Exportação em PDF (placeholder)

function exportarPDF(elementId) {
  const element = document.getElementById(elementId);
  if (element) {
    alert("Exportando conteúdo em PDF...");
    // Aqui pode integrar com jsPDF ou html2pdf
  }
}
